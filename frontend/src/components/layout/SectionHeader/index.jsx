import "./index.less"
import { Layout } from "antd";

const { Header } = Layout;

const SectionHeader = () => {
  return (
    <Header
      className="section-header primary-background"
    >
    </Header>
  );
};

export default SectionHeader;
